import React, { useState, useEffect, useRef } from 'react';
import PropTypes from 'prop-types';

// Components
import Container from '../../../components/layout/Container';

const Cashier = ({}) => {
  return <Container>cashier page</Container>;
};

Cashier.propTypes = {};

export default Cashier;
