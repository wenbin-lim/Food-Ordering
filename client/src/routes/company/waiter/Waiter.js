import React, { useState, useEffect, useRef } from 'react';
import PropTypes from 'prop-types';

// Components
import Container from '../../../components/layout/Container';

const Waiter = ({}) => {
  return <Container>waiter page</Container>;
};

Waiter.propTypes = {};

export default Waiter;
